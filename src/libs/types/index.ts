export type TStatistic = {
  title: string;
  number: number;
}

export type TRecentScore = {
  user: string;
  score: number;
  date: string;
}

export type TUserResult = {
  user: string;
  score: number;
  date: string;
}